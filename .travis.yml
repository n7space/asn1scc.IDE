language: cpp

os:
  - linux
  - osx

compiler:
  - gcc
  - clang

matrix:
  exclude:
  - os: osx
    compiler: gcc
  - os: linux
    compiler: clang

dist: trusty
sudo: required

env:
  global:
    - ENV_QTC_VERSION="4.3.1"
    - ENV_QTC_VERSION_SHORT="4.3"
  matrix:
    - ENV_WITH_TESTS=1
    - ENV_WITH_TESTS=0

addons:
  apt:
    sources:
    - sourceline: "ppa:beineri/opt-qt59-trusty"
    - ubuntu-toolchain-r-test
    packages:
    - g++-6
    - gcc-6
    - qt59-meta-minimal
    - qt59tools
    - p7zip
    - libstdc++6
    - libgl1-mesa-dev

install:
  - travis/install.sh

script:
  - travis/script.sh
